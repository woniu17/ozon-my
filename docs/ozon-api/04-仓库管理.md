# 仓库管理

> Ozon Seller API — 仓库管理
>
> 基础地址：`https://api-seller.ozon.ru`
>
> 认证请求头：`Client-Id`、`Api-Key`、`Content-Type: application/json`

---

## 目录

**FBS 和 rFBS 仓库管理**（9）

- `POST` `/v1/warehouse/list` — 仓库清单
- `POST` `/v1/delivery-method/list` — 仓库物流方式清单
- `POST` `/v2/delivery-method/list` — realFBS仓库的配送方式列表
- `POST` `/v2/warehouse/list` — 仓库列表
- `POST` `/v1/warehouse/operation/status` — 获取操作状态
- `POST` `/v1/warehouse/archive` — 将仓库归档
- `POST` `/v1/warehouse/unarchive` — 将仓库解除归档
- `POST` `/v1/warehouse/invalid-products/get` — 获取配送受限商品列表
- `POST` `/v1/warehouse/warehouses-with-invalid-products` — 获取含有配送受限商品的仓库列表

**创建并管理 FBS 仓库**（9）

- `POST` `/v1/warehouse/fbs/create/drop-off/list` — 获取用于创建仓库的揽收点列表
- `POST` `/v1/warehouse/fbs/update/drop-off/list` — 获取用于修改仓库信息的揽收点列表
- `POST` `/v1/warehouse/fbs/create/drop-off/timeslot/list` — 获取用于创建drop-off发运仓库的时间段列表
- `POST` `/v1/warehouse/fbs/update/drop-off/timeslot/list` — 获取用于更新drop-off发运仓库的时间段列表
- `POST` `/v1/warehouse/fbs/create/pick-up/timeslot/list` — 获取用于创建pick-up发运仓库的时间段列表
- `POST` `/v1/warehouse/fbs/update/pick-up/timeslot/list` — 获取用于更新pick-up发运仓库的时间段列表
- `POST` `/v1/warehouse/fbs/create` — 创建仓库
- `POST` `/v1/warehouse/fbs/update` — 更新仓库
- `POST` `/v1/warehouse/fbs/first-mile/update` — 更新头程物流

**多边形**（2）

- `POST` `/v1/polygon/create` — 创建一个快递的设施
- `POST` `/v1/polygon/bind` — 将快递方式与快递设施联系起来

---

## FBS 和 rFBS 仓库管理

### `POST` `/v1/warehouse/list`

**仓库清单**

`operationId: WarehouseAPI_WarehouseList`

该方式已过时，并将于2026年4月7日关闭。请切换至 /v2/warehouse/list 新版本。  方法返回 FBS 和 rFBS 仓库列表。如需获取 FBO 仓库列表，请使用方法[/v1/cluster/list](https://docs.ozon.ru/api/seller/en/#operation/SupplyDraftAPI_DraftClusterList)。

#### 请求体（`v1WarehouseListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| limit | integer | 是 | 响应中返回的值数量。 |
| offset | integer | 否 | 在响应中将被跳过的项目数量。例如，如果 `offset = 10`，则响应将从第 11 个找到的项目开始。 |
| with | object | 否 | 需要添加到响应中的附加字段。 |
|   with.able_to_set_price | boolean | 否 | `true`，用于在响应中添加关于价格设置能力的信息。 |

#### 响应（`warehouseWarehouseListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| has_entrusted_acceptance | boolean | 否 | 会表明受信任的接受。`true`, 如果库存中启用了受信任的接受方式。 |
| is_rfbs | boolean | 否 | 在 rFBS 计划下运作的仓库的标志： - `true` — 仓库在 rFBS 计划下运行； - `false` — 仓库没有在 rFBS 计划下运行。 |
| name | string | 否 | 仓库名称。 |
| warehouse_id | integer | 否 | 仓库识别号。 |
| can_print_act_in_advance | boolean | 否 | 有可能提前打印收发证书。 `true`, 如果可以提前打印的话。 |
| first_mile_type | object | 否 | 第一英里 FBS。 |
|   first_mile_type.dropoff_point_id | string | 否 | DropOff-点识别号。 |
|   first_mile_type.dropoff_timeslot_id | integer | 否 | DropOff临时存档识别号。 |
|   first_mile_type.first_mile_is_changing | boolean | 否 | 正在更新仓库设置的迹象。 |
|   first_mile_type.first_mile_type | string | 否 | 第一英里的类型 — `DropOff` 或 `Pickup`。 |
| has_postings_limit | boolean | 否 | 该迹象表明对最小订单数有限制。`true`, 如果有限制。 |
| is_karantin | boolean | 否 | 该迹象表明仓库因隔离而停止运作。 |
| is_kgt | boolean | 否 | 该迹象表明仓库接受大宗商品。 |
| is_able_to_set_price | boolean | 否 | 如果可以设置价格，则为`true`。 |
| is_presorted | boolean | 否 | 如果发运是预先分拣的，则为`true`。 |
| is_timetable_editable | boolean | 否 | 该迹象表明可以改变仓库运行时间表。 |
| min_postings_limit | integer | 否 | 限制的最小值是指在一次供货中可以带来的订单数量。 |
| postings_limit | integer | 否 | 极限值。 `-1`, 如果没有限制 |
| min_working_days | integer | 否 | 仓库运行天数。 |
| status | string | 否 | 仓库状况。  仓库状态与个人账户中的状态的对应关系:  \| 状态 Seller&nbsp;API \| 个人账户中的状态 \| \|---\|---\| \| `new` \| 正在激活 \| \| `created` \| 已激活 \| \| `disabled` \| 存档 \| \| `blocked` \| 已封禁 \| \| `disabled_due_to_limit` \| 暂停中 \| \| `error` \| 错误 \| |
| working_days |  | 否 | 仓库运行天数。 |

---

### `POST` `/v1/delivery-method/list`

**仓库物流方式清单**

`operationId: WarehouseAPI_DeliveryMethodList`

该方式已过时，并将于2026年4月7日关闭。请切换至 /v2/delivery-method/list 新版本。

#### 请求体（`warehouseDeliveryMethodListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| filter | object | 否 | 查找快递方式的过滤器。 |
|   filter.provider_id | integer | 否 | 快递服务识别号。 |
|   filter.status | string | 否 | 快递方式状态:   - `NEW` — 已创建,   - `EDITED` — 正在编辑,   - `ACTIVE` — 已激活,   - `DISABLED` — 未激活。 |
|   filter.warehouse_id | integer | 否 | 仓库识别号。可以使用方法 [/v1/warehouse/list](#operation/WarehouseAPI_WarehouseList)获取。 |
| limit | integer | 是 | 回答中的元素数量。最多50，最少1。 |
| offset | integer | 否 | 回答中会被略过的元素数量。例如，如果`offset = 10`，回答将从发现的第11个元素开始。 |

#### 响应（`warehouseDeliveryMethodListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| company_id | integer | 否 | 卖家识别号。 |
| created_at | string | 否 | 创建快递方式的日期和时间。 |
| cutoff | string | 否 | 卖方必须在此之前备货的时间。 |
| id | integer | 否 | 快递方式识别号。 |
| name | string | 否 | 快递方式名称。 |
| provider_id | integer | 否 | 快递服务识别号。 |
| sla_cut_in | integer | 否 | 根据仓库设置，订单备货的最短时间（以分钟为单位）。 |
| status | string | 否 | 快递方式状态:    - `NEW` — 已创建,    - `EDITED` — 正在编辑,    - `ACTIVE` — 已激活,    - `DISABLED` — 未激活。 |
| template_id | integer | 否 | 订单快递服务识别号。 |
| updated_at | string | 否 | 快递方式最后更新的日期和时间。 |
| warehouse_id | integer | 否 | 仓库识别号。 |

---

### `POST` `/v2/delivery-method/list`

**realFBS仓库的配送方式列表**

`operationId: WarehouseAPI_DeliveryMethodListV2`

#### 请求体（`v2DeliveryMethodListV2Request`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cursor | string | 否 | 用于选择下一批数据的指针。 |
| filter | object | 否 | 用于搜索配送方式的筛选条件。 |
|   filter.delivery_method_ids | array<string> | 否 | 配送方式标识符。 |
|   filter.provider_ids | array<string> | 否 | 配送服务标识符。 |
|   filter.status | array<string> | 否 | 配送方式状态： - `NEW`：已创建， - `EDITED`：编辑中， - `ACTIVE`：已启用， - `DISABLED`：未启用， - `WAITING`：审核中， - `BROKEN`：异常。 |
|   filter.warehouse_ids | array<string> | 否 | 仓库标识符。可通过方法[/v2/warehouse/list](#operation/WarehouseListV2)获取。 |
| limit | integer | 是 | 响应中返回的值数量。 |
| sort_dir | string | 否 | 排序方向： - `ASC`：升序， - `DESC`：降序。 |

#### 响应（`v2DeliveryMethodListV2Response`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cursor | string | 否 | 用于选择下一批数据的指针。 |
| has_next | boolean | 否 | `true`，表示响应中未返回全部配送方式。 |
| delivery_methods | array<object> | 否 | 配送方式。 |
|   delivery_methods[].created_at | string | 否 | 配送方式创建日期。 |
|   delivery_methods[].cutoff | string | 否 | 卖家需在此时间前完成订单备货。 |
|   delivery_methods[].id | integer | 否 | 配送方式标识符。 |
|   delivery_methods[].is_express | boolean | 否 | `true`，表示可使用Ozon Express快速配送。 |
|   delivery_methods[].name | string | 否 | 配送方式名称。 |
|   delivery_methods[].provider_id | integer | 否 | 配送服务标识符。 |
|   delivery_methods[].sla_cut_in | integer | 否 | 根据仓库设置，订单备货的最短时间（以分钟为单位）。 |
|   delivery_methods[].status | string | 否 | 配送方式状态： - `NEW`：已创建， - `EDITED`：编辑中， - `ACTIVE`：已启用， - `DISABLED`：未启用， - `WAITING`：审核中， - `BROKEN`：异常。 |
|   delivery_methods[].template_id | integer | 否 | 订单配送服务标识符。 |
|   delivery_methods[].tpl_dropoff_point | object | 否 | 揽收点信息。 |
|     delivery_methods[].tpl_dropoff_point.address | string | 否 | 揽收点地址。 |
|     delivery_methods[].tpl_dropoff_point.address_coordinates | object | 否 | 揽收点坐标。 |
|       delivery_methods[].tpl_dropoff_point.address_coordinates.latitude | number | 否 | 纬度。 |
|       delivery_methods[].tpl_dropoff_point.address_coordinates.longitude | number | 否 | 经度。 |
|     delivery_methods[].tpl_dropoff_point.code | string | 否 | 运输公司系统中的揽收点代码。 |
|     delivery_methods[].tpl_dropoff_point.name | string | 否 | 揽收点名称。 |
|   delivery_methods[].tpl_integration_type | string | 否 | 与配送服务的集成类型： - `aggregator`：由第三方配送，Ozon负责登记订单； - `3pl_tracking`：由第三方配送，卖家负责登记订单； - `non_integrated`：由卖家自行配送； - `hybrid`：混合集成。 |
|   delivery_methods[].updated_at | string | 否 | 配送方式最后更新日期和时间。 |
|   delivery_methods[].warehouse_id | integer | 否 | 仓库标识符。 |

---

### `POST` `/v2/warehouse/list`

**仓库列表**

`operationId: WarehouseListV2`

该方式返回 FBS 和 rFBS 仓库列表。如要获取 FBO 仓库列表，请使用 [/v1/warehouse/fbo/list](https://docs.ozon.ru/api/seller/en/#operation/SupplyDraftAPI_DraftGetWarehouseFboList) 方式。

#### 请求体（`v2WarehouseListV2Request`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| limit | integer | 是 | 响应中返回的值数量。 |
| cursor | string | 否 | 后续数据的选择标志。 |
| warehouse_ids | array<string> | 否 | 仓库识别符。 |

#### 响应（`v2WarehouseListV2Response`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cursor | string | 否 | 后续数据的选择标志。 |
| warehouses | array<object> | 否 | 仓库列表。 |
|   warehouses[].address_info | object | 否 | 仓库位置信息。 |
|     warehouses[].address_info.address | string | 否 | 仓库地址。 |
|     warehouses[].address_info.latitude | number | 否 | 纬度。 |
|     warehouses[].address_info.longitude | number | 否 | 经度。 |
|     warehouses[].address_info.utc | string | 否 | 时区。 |
|   warehouses[].carriage_label_type | string | 否 | 标签类型： - `UNSPECIFIED` — 未知类型； - `BIG` — 大标签； - `SMALL` — 小标签。 |
|   warehouses[].courier_comment | string | 否 | 给快递员的评论。 |
|   warehouses[].courier_phones | array<string> | 否 | 用于与快递员联系的电话号码。 |
|   warehouses[].created_at | string | 否 | 仓库创建日期和时间。 |
|   warehouses[].cut_in_time | integer | 否 | 发运所需时间（分钟）。 |
|   warehouses[].first_mile | object | 否 | 头程物流。 |
|     warehouses[].first_mile.dropoff_point_id | string | 否 | 揽收点识别符。 |
|     warehouses[].first_mile.first_mile_is_changing | boolean | 否 | 仓库设置更新的标识。 |
|     warehouses[].first_mile.timeslot_from | string | 否 | 时间段开始时间。 |
|     warehouses[].first_mile.timeslot_id | integer | 否 | 时间段识别符。 |
|     warehouses[].first_mile.timeslot_to | string | 否 | 时间段结束时间。 |
|     warehouses[].first_mile.type | string | 否 | 头程物流类型 — `PICK_UP` 或 `DROP_OFF`。 |
|   warehouses[].has_entrusted_acceptance | boolean | 否 | 信任验收开通标识。 |
|   warehouses[].has_postings_limit | boolean | 否 | 存在最低订单数量限额的标识：若有限额，则返回值为 `true`。 |
|   warehouses[].is_auto_assembly | boolean | 否 | 自动备货开启的标识。 |
|   warehouses[].is_comfort | boolean | 否 | сomfort配送标志。送达买家时间大于等于60分钟。 |
|   warehouses[].is_express | boolean | 否 | express配送标志。送达买家时间不超过60分钟。 |
|   warehouses[].is_kgt | boolean | 否 | 仓库接收超大货物的标识。 |
|   warehouses[].is_rfbs | boolean | 否 | 仓库在 rFBS 工作模式下工作的标识。 |
|   warehouses[].is_waybill_enabled | boolean | 否 | 运单打印开启的标识。 |
|   warehouses[].min_postings_limit | integer | 否 | 一次交货中可以运来的最低订单数量。 |
|   warehouses[].name | string | 否 | 仓库名称。 |
|   warehouses[].phone | string | 否 | 仓库电话号码。 |
|   warehouses[].postings_limit | integer | 否 | 订单限额。如果没有限额，则返回值为 `-1`。 |
|   warehouses[].sla_cut_in | integer | 否 | 以分钟为单位的订单备货最低时间。 |
|   warehouses[].status | string | 否 | 仓库状态。 |
|   warehouses[].timetable | object | 否 | 仓库工作时间表。 |
|     warehouses[].timetable.timetable_from | string | 否 | 仓库工作开始日期。 |
|     warehouses[].timetable.timetable_to | string | 否 | 仓库工作结束日期。 |
|     warehouses[].timetable.working_hours | array<object> | 否 | 仓库工作时间。 |
|       warehouses[].timetable.working_hours[].time_from | string | 否 | 工作开始时间。 |
|       warehouses[].timetable.working_hours[].time_to | string | 否 | 工作结束时间。 |
|   warehouses[].updated_at | string | 否 | 仓库信息最后一次更新的日期和时间。 |
|   warehouses[].warehouse_id | integer | 否 | 仓库识别符。 |
|   warehouses[].warehouse_type | string | 否 | 仓库类型。 |
|   warehouses[].with_item_list | boolean | 否 | 拣货单打印开启的标识。 |
|   warehouses[].working_days | array<string> | 否 | 仓库工作日： - `UNSPECIFIED` — 值未确定； - `MONDAY` — 星期一； - `TUESDAY` — 星期二； - `WEDNESDAY` — 星期三； - `THURSDAY` — 星期四； - `FRIDAY` — 星期五； - `SATURDAY` — 星期六； - `SUNDAY` — 星期日。 |
| has_next | boolean | 否 | `true`，前提是本次响应未返回所有数据。 |

---

### `POST` `/v1/warehouse/operation/status`

**获取操作状态**

`operationId: GetWarehouseFBSOperationStatus`

#### 请求体（`v1GetWarehouseFBSOperationStatusRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 是 | 操作ID。 |

#### 响应（`v1GetWarehouseFBSOperationStatusResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| entity_id | integer | 否 | 正在处理的实体ID。如果操作为 `CREATE_FBS_WAREHOUSE`，则返回仓库ID。 |

---

### `POST` `/v1/warehouse/archive`

**将仓库归档**

`operationId: ArchiveWarehouseFBS`

#### 请求体（`v1ArchiveWarehouseFBSRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| reason | string | 是 | 仓库归档原因。 |
| warehouse_id | integer | 是 | 仓库识别符。 |

#### 响应（`v1ArchiveWarehouseFBSResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 否 | 操作识别符。请使用 [/v1/warehouse/operation/status](#operation/GetWarehouseFBSOperationStatus) 方式获取操作状态。 |

---

### `POST` `/v1/warehouse/unarchive`

**将仓库解除归档**

`operationId: UnarchiveWarehouseFBS`

#### 请求体（`v1UnarchiveWarehouseFBSRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| warehouse_id | integer | 是 | 仓库识别符。 |

#### 响应（`v1UnarchiveWarehouseFBSResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 否 | 操作识别符。请使用 [/v1/warehouse/operation/status](#operation/GetWarehouseFBSOperationStatus) 方式获取操作状态。 |

---

### `POST` `/v1/warehouse/invalid-products/get`

**获取配送受限商品列表**

`operationId: WarehouseInvalidProductsGet`

#### 请求体（`v1WarehouseInvalidProductsGetRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| last_id | integer | 否 | 页面上最后一个值的标识符。首次请求时请将此字段留空。  如需获取后续数据，请填写上次响应中的 `last_id`。 |
| warehouse_id | integer | 是 | 仓库标识符。请通过方法[/v1/warehouse/warehouses-with-invalid-products](#operation/WarehouseWithInvalidProducts)获取该参数值。 |

#### 响应（`v1WarehouseInvalidProductsGetResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| has_next | boolean | 否 | 若响应中未包含全部商品，则为`true`。 |
| last_id | integer | 否 | 页面上最后一个值的标识符。要获取下一个批次的数据，请在下一个请求的 `last_id` 参数中传递上次获取的值。 |
| validation_results | array<object> | 否 | 检查结果。 |
|   validation_results[].item | object | 否 | 商品信息。 |
|     validation_results[].item.size | object | 否 | 商品尺寸信息。 |
|       validation_results[].item.size.height_mm | integer | 否 | 商品高度（毫米）。 |
|       validation_results[].item.size.length_mm | integer | 否 | 商品长度（毫米）。 |
|       validation_results[].item.size.width_mm | integer | 否 | 商品宽度（毫米）。 |
|     validation_results[].item.sku | integer | 否 | 商品在Ozon系统中的标识符——SKU。 |
|     validation_results[].item.weight_g | number | 否 | 商品重量（克）。 |
|   validation_results[].state | string | 否 | 检查状态： - `UNSPECIFIED`——未指定； - `NOT_VALID`——商品未通过检查。 |
|   validation_results[].validation_errors | array<object> | 否 | 错误信息。 |
|     validation_results[].validation_errors[].characteristic | string | 否 | 未通过检查的商品特征。    可能的限制类型： - `UNSPECIFIED`——未指定； - `LENGTH`——长度； - `WIDTH`——宽度； - `HEIGHT`——高度； - `WEIGHT`——重量； - `SUM_OF_DIMENSIONS`——尺寸总和； - `VOLUME_WEIGHT`——体积重量； - `VOLUME`——体积； - `PRICE`——价格； - `LON... |
|     validation_results[].validation_errors[].restriction_price | object | 否 | 价格限制。 |
|       validation_results[].validation_errors[].restriction_price.currency | string | 否 | 货币单位。 |
|       validation_results[].validation_errors[].restriction_price.value | number | 否 | 价格数值。 |
|     validation_results[].validation_errors[].restriction_vwc | number | 否 | 体积和重量特征限制值——体积和重量特征。 |
|     validation_results[].validation_errors[].template_id | integer | 否 | 设置配送限制的订单配送服务 ID。 |
|     validation_results[].validation_errors[].type | string | 否 | 错误类型：  - `UNSPECIFIED`——未指定；  - `LESS_THAN_MIN`——小于最小值；  - `GREATER_THAN_MAX`——大于最大值。 |
| warehouse_id | integer | 否 | 仓库标识符。 |

---

### `POST` `/v1/warehouse/warehouses-with-invalid-products`

**获取含有配送受限商品的仓库列表**

`operationId: WarehouseWithInvalidProducts`

返回存在配送限制商品的仓库标识符。

#### 响应（`v1WarehouseWithInvalidProductsResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| warehouse_ids | array<string> | 否 | 包含至少 1 件无法从该仓库发运的受限商品的仓库标识符列表。如需获取受限商品列表，请使用方法 [/v1/warehouse/invalid-products/get](#operation/WarehouseInvalidProductsGet)。 |

---

## 创建并管理 FBS 仓库

### `POST` `/v1/warehouse/fbs/create/drop-off/list`

**获取用于创建仓库的揽收点列表**

`operationId: WarehouseAPI_ListDropOffPointsForCreateFBSWarehouse`

#### 请求体（`v1ListDropOffPointsForCreateFBSWarehouseRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| coordinates | object | 否 | 坐标。 |
|   coordinates.latitude | number | 是 | 纬度。 |
|   coordinates.longitude | number | 是 | 经度。 |
| country_code | string | 是 | ISO 2格式的国家代码。 |
| is_kgt | boolean | 是 | `true`表示商品为超大。 |
| search | object | 否 | 搜索参数。 |
|   search.address | string | 否 | 揽收点地址。 |
|   search.types | array<string> | 否 | 揽收点类型： - `PVZ` — 订单取货点， - `PPZ` — 订单接收点， - `SC` — 分拣中心。 |

#### 响应（`v1ListDropOffPointsForCreateFBSWarehouseResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| points | array<object> | 否 | 点位列表。 |
|   points[].address | string | 否 | 揽收点地址。 |
|   points[].coordinates | object | 否 | 揽收点坐标。 |
|     points[].coordinates.latitude | number | 否 | 纬度。 |
|     points[].coordinates.longitude | number | 否 | 经度。 |
|   points[].discount_percent | number | 否 | 交付货件的折扣百分比。 |
|   points[].id | string | 否 | 揽收点标识符。 |
|   points[].last_transit_time_local | object | 否 | 为获得发运折扣，需要在此时间前交付货件。 |
|     points[].last_transit_time_local.hours | integer | 否 | 小时。 |
|     points[].last_transit_time_local.minutes | integer | 否 | 分钟。 |
|     points[].last_transit_time_local.nanos | integer | 否 | 纳秒。 |
|     points[].last_transit_time_local.seconds | integer | 否 | 秒。 |
|   points[].type | string | 否 | D揽收点类型： - `PVZ` — 订单取货点， - `PPZ` — 订单接收点， - `SC` — 分拣中心。 |

---

### `POST` `/v1/warehouse/fbs/update/drop-off/list`

**获取用于修改仓库信息的揽收点列表**

`operationId: WarehouseAPI_ListDropOffPointsForUpdateFBSWarehouse`

#### 请求体（`v1ListDropOffPointsForUpdateFBSWarehouseRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| search | object | 否 | 搜索参数。 |
|   search.address | string | 否 | 根据揽收点地址搜索。 |
|   search.types | array<string> | 否 | 揽收点类型： - `PVZ` — 订单取货点， - `PPZ` — 订单接收点， - `SC` — 分拣中心。 |
| warehouse_id | integer | 是 | 现有FBS仓库的筛选条件。 |

#### 响应（`v1ListDropOffPointsForUpdateFBSWarehouseResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| points | array<object> | 否 | 点位列表。 |
|   points[].address | string | 否 | 揽收点地址。 |
|   points[].coordinates | object | 否 | 揽收点坐标。 |
|     points[].coordinates.latitude | number | 否 | 纬度。 |
|     points[].coordinates.longitude | number | 否 | 经度。 |
|   points[].discount_percent | number | 否 | 交付货件的折扣百分比。 |
|   points[].id | string | 否 | 揽收点标识符。 |
|   points[].last_transit_time_local | object | 否 | 为获得发运折扣，需要在此时间前交付货件。 |
|     points[].last_transit_time_local.hours | integer | 否 | 小时。 |
|     points[].last_transit_time_local.minutes | integer | 否 | 分钟。 |
|     points[].last_transit_time_local.nanos | integer | 否 | 纳秒。 |
|     points[].last_transit_time_local.seconds | integer | 否 | 秒。 |
|   points[].type | string | 否 | 揽收点类型：   - `PVZ` — 订单取货点，   - `PPZ` — 订单接收点，   - `SC` — 分拣中心。 |

---

### `POST` `/v1/warehouse/fbs/create/drop-off/timeslot/list`

**获取用于创建drop-off发运仓库的时间段列表**

`operationId: WarehouseFbsCreateDropOffTimeslotList`

#### 请求体（`v1WarehouseFbsCreateDropOffTimeslotListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| drop_off_point_id | integer | 是 | 揽收点标识符。 |

#### 响应（`v1WarehouseFbsCreateDropOffTimeslotListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| timeslots | array<object> | 否 | 时间段列表。 |
|   timeslots[].acceptance_end_time_local | string | 否 | 订单接收结束本地时间。 |
|   timeslots[].acceptance_start_time_local | string | 否 | 订单接收开始本地时间。 |
|   timeslots[].from | string | 否 | 时间段开始时间。 |
|   timeslots[].id | integer | 否 | 时间段标识符。 |
|   timeslots[].to | string | 否 | 时间段结束时间。 |

---

### `POST` `/v1/warehouse/fbs/update/drop-off/timeslot/list`

**获取用于更新drop-off发运仓库的时间段列表**

`operationId: WarehouseFbsUpdateDropOffTimeslotList`

#### 请求体（`v1WarehouseFbsUpdateDropOffTimeslotListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| drop_off_point_id | integer | 是 | 揽收点标识符。 |
| warehouse_id | integer | 是 | 仓库标识符。 |

#### 响应（`v1WarehouseFbsUpdateDropOffTimeslotListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| timeslots | array<object> | 否 | 时间段列表。 |
|   timeslots[].acceptance_end_time_local | string | 否 | Sipariş kabul işleminin sona erdiği yerel saat. |
|   timeslots[].acceptance_start_time_local | string | 否 | Sipariş kabul işleminin başladığı yerel saat. |
|   timeslots[].from | string | 否 | Zaman aralığı başlangıç saati. |
|   timeslots[].id | integer | 否 | Zaman aralığı kimliği. |
|   timeslots[].to | string | 否 | Zaman aralığı bitiş saati. |

---

### `POST` `/v1/warehouse/fbs/create/pick-up/timeslot/list`

**获取用于创建pick-up发运仓库的时间段列表**

`operationId: WarehouseFbsCreatePickUpTimeslotList`

#### 请求体（`v1WarehouseFbsCreatePickUpTimeslotListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| address_coordinates | object | 是 | 仓库坐标。 |
|   address_coordinates.latitude | number | 是 | 纬度。 |
|   address_coordinates.longitude | number | 是 | 经度。 |
| is_kgt | boolean | 是 | 超大货物标志。 |

#### 响应（`v1WarehouseFbsCreatePickUpTimeslotListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| is_pickup_supported | boolean | 否 | 支持pick-up发运标志。 |
| timeslots | array<object> | 否 | 时间段列表。 |
|   timeslots[].from | string | 否 | 时间段开始时间。 |
|   timeslots[].id | integer | 否 | 时间段标识符。 |
|   timeslots[].to | string | 否 | 时间段结束时间。 |

---

### `POST` `/v1/warehouse/fbs/update/pick-up/timeslot/list`

**获取用于更新pick-up发运仓库的时间段列表**

`operationId: WarehouseFbsUpdatePickUpTimeslotList`

#### 请求体（`v1WarehouseFbsUpdatePickUpTimeslotListRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| warehouse_id | integer | 是 | 仓库标识符。 |

#### 响应（`v1WarehouseFbsUpdatePickUpTimeslotListResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| timeslots | array<object> | 否 | 时间段列表。 |
|   timeslots[].from | string | 否 | 时间段开始时间。 |
|   timeslots[].id | integer | 否 | 时间段标识符。 |
|   timeslots[].to | string | 否 | 时间段结束时间。 |

---

### `POST` `/v1/warehouse/fbs/create`

**创建仓库**

`operationId: WarehouseAPI_CreateWarehouseFBS`

如果创建带有揽收点配送的仓库，请使用方法[/v1/warehouse/fbs/create/drop-off/list](#operation/WarehouseAPI_ListDropOffPointsForCreateFBSWarehouse)获取点位列表。

#### 请求体（`v1CreateWarehouseFBSRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| address_coordinates | object | 是 | 仓库地址坐标。 |
|   address_coordinates.latitude | number | 是 | 纬度。 |
|   address_coordinates.longitude | number | 是 | 经度。 |
| cut_in_time | integer | 是 | 接单时间（分钟）。例如，如果传入 `3000`，则接单将在传入后50小时内结束。 |
| drop_off_point_id | integer | 否 | 揽收点标识符。 |
| first_mile_type | string | 是 | 头程物流类型： - `PICK_UP` — 订单交给快递员； - `DROP_OFF` — 订单送到接收点。 |
| is_kgt | boolean | 是 | `true`表示商品为超大。 |
| name | string | 是 | 仓库名称。 |
| options | object | 否 | 仓库参数。 |
|   options.comment | string | 否 | 交货类型为 `PICK_UP` 时给快递员的备注。 |
|   options.courier_phones | array<string> | 否 | 交货类型为 `PICK_UP` 时给快递员的电话号码。请按格式填写：+7 (XXX) XXX-XX-XX。 |
|   options.is_auto_assembly | boolean | 否 | `true`表示启用自动备货。 |
|   options.is_waybill_enabled | boolean | 否 | `true`表示启用运单打印。 |
| phone | string | 是 | 仓库联系电话。请按格式填写：+7 (XXX) XXX-XX-XX。 |
| timeslot_id | integer | 是 | 时间段标识符。 |
| working_days | array<string> | 否 | 仓库工作日： - `MONDAY` — 星期一， - `TUESDAY` — 星期二，  - `WEDNESDAY` — 星期三， - `THURSDAY` — 星期四， - `FRIDAY` — 星期五， - `SATURDAY` — 星期六， - `SUNDAY` — 星期天。 |

#### 响应（`v1CreateWarehouseFBSResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 否 | 创建FBS仓库的操作标识符。要获取操作状态，请使用方法 [/v1/warehouse/operation/status](#operation/GetWarehouseFBSOperationStatus)。 |

---

### `POST` `/v1/warehouse/fbs/update`

**更新仓库**

`operationId: UpdateWarehouseFBS`

#### 请求体（`v1UpdateWarehouseFBSRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| address_coordinates | object | 是 | 仓库坐标。 |
|   address_coordinates.latitude | number | 是 | 纬度。 |
|   address_coordinates.longitude | number | 是 | 经度。 |
| name | string | 否 | 仓库名称。 |
| options | object | 否 | 仓库参数。 |
|   options.comment | string | 否 | 发运类型为 `PICK_UP` 时，给快递员的备注。 |
|   options.courier_phones | array<string> | 否 | 发运类型为 `PICK_UP` 时，快递员联系电话。 |
|   options.is_auto_assembly | boolean | 否 | 启用自动备货标识。 |
|   options.is_waybill_enabled | boolean | 否 | 启用运单打印标识。 |
| phone | string | 否 | 仓库电话号码。 |
| warehouse_id | integer | 是 | 仓库ID。 |
| working_days | array<string> | 否 | 仓库工作日： - `MONDAY` — 周一； - `TUESDAY` — 周二； - `WEDNESDAY` — 周三； - `THURSDAY` — 周四； - `FRIDAY` — 周五； - `SATURDAY` — 周六； - `SUNDAY` — 周日。 |

#### 响应（`v1UpdateWarehouseFBSResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 否 | 操作ID。通过方法 [/v1/warehouse/operation/status](#operation/GetWarehouseFBSOperationStatus) 获取操作状态。 |

---

### `POST` `/v1/warehouse/fbs/first-mile/update`

**更新头程物流**

`operationId: UpdateWarehouseFBSFirstMile`

#### 请求体（`v1UpdateWarehouseFBSFirstMileRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cut_in_time | integer | 是 | 接单时间（分钟）。例如，如果传入 `3000`，则接单将在传入后50小时内结束。 |
| drop_off_point_id | integer | 否 | 订单发运点ID。如果` first_mile_type = DROP_OFF`，该参数必填。 |
| first_mile_type | string | 是 | 第一英里类型： - `PICK_UP` —发运给快递员； - `DROP_OFF` —发运至接收点。 |
| timeslot_id | integer | 是 | 时间段标识符。 |
| warehouse_id | integer | 是 | 仓库ID。 |

#### 响应（`v1UpdateWarehouseFBSFirstMileResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| operation_id | string | 否 | 操作ID。通过方法 [/v1/warehouse/operation/status](#operation/GetWarehouseFBSOperationStatus) 获取操作状态。 |

---

## 多边形

### `POST` `/v1/polygon/create`

**创建一个快递的设施**

`operationId: PolygonAPI_CreatePolygon`

你可以为快递方式添加一个设施。  通过在https://geojson.io，获取其坐标来创建一个设施：在地图上至少标记3个点，并用线连接起来。  服务geojson.io返回格式为[[[long lat]]]的坐标。请在方法请求中调换纬度和经度的位置。

#### 请求体（`polygonv1PolygonCreateRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| coordinates | string | 是 | 快递设施的坐标，格式为 `[[[lat long]]]`。 |

#### 响应（`polygonv1PolygonCreateResponse`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| polygon_id | integer | 否 | 设施识别号。 |

---

### `POST` `/v1/polygon/bind`

**将快递方式与快递设施联系起来**

`operationId: PolygonAPI_BindPolygon`

#### 请求体（`polygonv1PolygonBindRequest`）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| delivery_method_id | integer | 是 | 快递方法识别号。 |
| polygons | array<object> | 是 | 设施清单。 |
|   polygons[].polygon_id | integer | 是 | 设施识别号。 |
|   polygons[].time | integer | 是 | 商品在该点快递到达的时间，以分钟计。 |
| warehouse_location | object | 是 | 仓库位置。 |
|   warehouse_location.lat | string | 是 | 仓库位置的地理纬度。 |
|   warehouse_location.lon | string | 是 | 仓库位置的地理经度。 |

#### 响应（`polygonv1Empty`）

---

